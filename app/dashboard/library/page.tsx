"use client"

import { Card, CardContent } from "@/components/ui/card"
import { Button } from "@/components/ui/button"
import { Tabs, TabsContent, TabsList, TabsTrigger } from "@/components/ui/tabs"
import { Library, FileText, Image, Code, Presentation, FolderOpen } from "lucide-react"

export default function LibraryPage() {
  const categories = [
    { icon: FileText, label: "Documents", count: 0 },
    { icon: Image, label: "Images", count: 0 },
    { icon: Code, label: "Code", count: 0 },
    { icon: Presentation, label: "Presentations", count: 0 },
  ]

  return (
    <div className="flex h-full flex-col px-6 py-8">
      <div className="mb-8 flex items-center justify-between">
        <h1 className="text-2xl font-semibold">Library</h1>
        <Button>Upload file</Button>
      </div>

      <Tabs defaultValue="all" className="flex-1">
        <TabsList>
          <TabsTrigger value="all">All files</TabsTrigger>
          <TabsTrigger value="documents">Documents</TabsTrigger>
          <TabsTrigger value="images">Images</TabsTrigger>
          <TabsTrigger value="code">Code</TabsTrigger>
        </TabsList>

        <TabsContent value="all" className="mt-6">
          {/* Categories */}
          <div className="mb-8 grid gap-4 md:grid-cols-4">
            {categories.map((category) => (
              <Card key={category.label} className="cursor-pointer border-border hover:border-primary/50 transition-colors">
                <CardContent className="flex items-center gap-4 p-4">
                  <div className="flex h-10 w-10 items-center justify-center rounded-lg bg-muted">
                    <category.icon className="h-5 w-5" />
                  </div>
                  <div>
                    <p className="font-medium">{category.label}</p>
                    <p className="text-sm text-muted-foreground">{category.count} files</p>
                  </div>
                </CardContent>
              </Card>
            ))}
          </div>

          {/* Empty State */}
          <div className="flex flex-col items-center justify-center rounded-lg border border-dashed border-border py-24">
            <FolderOpen className="h-16 w-16 text-muted-foreground/30" />
            <h2 className="mt-4 text-lg font-semibold">No files yet</h2>
            <p className="mt-2 text-sm text-muted-foreground">
              Files generated by WorkwithMe will appear here
            </p>
            <Button className="mt-6" variant="outline">
              Create your first task
            </Button>
          </div>
        </TabsContent>

        <TabsContent value="documents" className="mt-6">
          <div className="flex flex-col items-center justify-center rounded-lg border border-dashed border-border py-24">
            <FileText className="h-16 w-16 text-muted-foreground/30" />
            <h2 className="mt-4 text-lg font-semibold">No documents yet</h2>
            <p className="mt-2 text-sm text-muted-foreground">
              Documents will appear here when created
            </p>
          </div>
        </TabsContent>

        <TabsContent value="images" className="mt-6">
          <div className="flex flex-col items-center justify-center rounded-lg border border-dashed border-border py-24">
            <Image className="h-16 w-16 text-muted-foreground/30" />
            <h2 className="mt-4 text-lg font-semibold">No images yet</h2>
            <p className="mt-2 text-sm text-muted-foreground">
              Images will appear here when created
            </p>
          </div>
        </TabsContent>

        <TabsContent value="code" className="mt-6">
          <div className="flex flex-col items-center justify-center rounded-lg border border-dashed border-border py-24">
            <Code className="h-16 w-16 text-muted-foreground/30" />
            <h2 className="mt-4 text-lg font-semibold">No code files yet</h2>
            <p className="mt-2 text-sm text-muted-foreground">
              Code files will appear here when created
            </p>
          </div>
        </TabsContent>
      </Tabs>
    </div>
  )
}
